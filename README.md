$HEDGE site — image requirements
Drop these three files into an images/ folder next to index.html (paths are already wired into the HTML, nothing else to edit):
Path
Suggested source
Recommended size
images/hero-hedgehog.png
The full-body suited hedgehog banner shot
≥ 800×800px, square crop looks best (the frame is a square card)
images/hedge-logo.png
The square hedgehog avatar (the one used as your profile/logo image)
128×128px or larger, square
images/favicon.png
Same square avatar, small
32×32px or 64×64px
Everything else (ticker, contract-address copy button, hedge-o-meter gauge, chart background, scroll reveals) is pure CSS/JS/canvas — no other assets needed.
